@inherits FullComponentBase

<MudIconButton Class="ml-n4" Icon="@IconsF.Close" Color="@(Layout.IsDarkMode ? Color.Default : Color.Dark)" OnClick="@(() => Dialog.Close())" />

@code {
    [Parameter] public MudDialogInstance Dialog { get; set; } = null!;
}