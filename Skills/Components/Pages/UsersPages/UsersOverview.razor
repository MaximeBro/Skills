@page "/overview"
@page "/overview/{Id}"
@page "/overview/{Id}/{TabIndex:int}"

<AuthorizedComponent Roles="Admin, Manager, User">
    <Content>
        <MudTabs Centered Color="Color.Primary" ActivePanelIndex="@(TabIndex ?? 0)">
            <SkillsProfile User="@_user" />
            <CvProfile User="@_user" />
        </MudTabs>
    </Content>
</AuthorizedComponent>
