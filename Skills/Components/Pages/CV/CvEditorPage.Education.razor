@inherits FullComponentBase

<MudPaper Class="mt-6" Elevation="2">
    <MudStack Class="px-4 py-3" Row="false" Spacing="2">
        <MudText Class="mark-one bold">Diplômes</MudText>
        <MudStack Class="px-6 py-2" Row="false">
            @foreach (var education in CvEducations.OrderByDescending(x => x.YearEnd))
            {
                <MudPaper Class="rounded-lg mb-1 d-flex px-4 py-1" Elevation="2" Width="100%" Style="flex-direction: row; align-items: center;">
                    <MudStack Row="false" Spacing="0">
                        <MudStack Row Justify="Justify.FlexStart" AlignItems="AlignItems.Center">
                            <MudText Class="mark-one bold" Color="Color.Primary" Align="Align.Left">@education.Title</MudText> <MudText Class="mark-one bold" Color="Color.Dark" Align="Align.Left">|</MudText> <MudText Class="mark-one bold" Align="Align.Left">@education.Supplier</MudText>
                        </MudStack>
                        <MudStack Row Justify="Justify.FlexStart" AlignItems="AlignItems.Center">
                            <MudText Class="mark-one">@education.YearStart</MudText> - <MudText Class="mark-one">@education.YearEnd</MudText> | <MudText Class="mark-one">@education.Town</MudText>
                        </MudStack>
                        @if (!string.IsNullOrWhiteSpace(education.Description))
                        {
                            <MudStack>
                                <MudText Class="mark-one" Typo="Typo.subtitle2">@education.Description</MudText>
                            </MudStack>
                        }
                    </MudStack>
                    <MudSpacer/>
                    <MudIconButton Class="hover-red" Icon="@IconsF.Delete" Size="Size.Small" OnClick="@(() => CvEducations.Remove(education))"/>
                </MudPaper>
            }
            <MudStack Row Justify="Justify.Center">
                <MudPaper Class="rounded-lg" Elevation="2" Width="100%" Style="@($"border: 2px dashed {ThemeManager.GetColor(Color.Primary)};")">
                    <MudStack Class="py-1" Row Justify="Justify.Center">
                        <MudTooltip Text="Ajouter un diplôme" Placement="Placement.Top" Arrow>
                            <MudIconButton Icon="@IconsR.AddCircle" Color="Color.Primary" Size="Size.Small" OnClick="@(async () => await CreateEducationAsync())"/>
                        </MudTooltip>
                    </MudStack>
                </MudPaper>
            </MudStack>
        </MudStack>
    </MudStack>
</MudPaper>